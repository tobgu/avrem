(ns avrem.bitmatch-test
  (:use avrem.bitmatch)
  (:use clojure.test)
  (:use midje.sweet))

; Tests defined together with the source right now


;(deftest a-test
;  (testing "FIXME, I fail."
;    (is (= 0 1))))